extends layout

block content
  h1.title= title
  ul.items
    each item in item_list
      li
        a(href=item.url)
          .item-card
            .item-img
              img(src=item.img, alt=item.name)
            h2.item-name=item.name
